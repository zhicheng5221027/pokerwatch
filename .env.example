# Public — safe to expose in the browser bundle (Vite VITE_* prefix)
VITE_SUPABASE_URL=https://tstbiifukjstepgscmki.supabase.co
VITE_SUPABASE_ANON_KEY=__set_me__

# SERVER ONLY — set these as Supabase Edge Function secrets, NEVER in the frontend.
# Add via:  supabase secrets set OPENAI_API_KEY=sk-...
# OPENAI_API_KEY=sk-...
